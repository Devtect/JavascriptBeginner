/*
    Arithmetic expressions are a combination of
    operands (values, variables, etc.)
    operator (+ - * / %)
    exampel y = x + 5
*/

let students = 20;

//students = students + 1;
//students = students - 1;
//students = students * 2;
//students = students / 2;
//let extra_students = students % 3;

// Augmented assignment operators   Use this to reassign the same variable
//students += 1;
//students -= 1;
//students *= 2;
//students /= 2;

/*
    Operator precedence
    1. Paranteshesis ()
    2. Expotents
    3. Multiplication & division
    4. Addition & subtraction
*/

//let result = 1 + 2 * (3 + 4);  compare to let result = (1 + 2) * (3 + 4);
console.log(result);
